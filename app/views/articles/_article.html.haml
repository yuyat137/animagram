%section.max-w-7xl.mx-auto.sm:px-6.lg:px-4.mt-12
  %section.mt-6.grid.md:grid-cols-2.lg:grid-cols-4.gap-x-6.gap-y-8
    %article.bg-white.group.relative.rounded-lg.overflow-hidden.shadow-lg.mt-8
      %h2.text-1xl.font-extrabold.text-gray-600.text-center.mt-16
        = link_to article.title, article_path(article)
      .relative.w-full.h-80.md:h-64.lg:h-44.flex.justify-center.mt-8
        = image_tag article.image
      .px-3.py-4
        %h3.text-sm.text-gray-500.pb-2
        %p.text-1xl.text-base.font-semibold.text-gray-600.group-hover:text-indigo-600.ml-8
          = safe_join(article.description.split("\n"))
        %p.text-1xl.text-base.font-semibold.text-gray-600.group-hover:text-indigo-600.text-right.mr-8
          = safe_join(article.user.name.split("\n"))
        .flex.flex-wrap.justify-center.my-8
          - if logged_in?
            - if current_user.own?(article)
              .text-1xl.text-yellow-500.my-4.mx-4
                = link_to icon("fas", "edit", class: "edit"), edit_article_path(article)
              .text-1xl.text-yellow-500.my-4.mx-4
                = link_to icon("fas", "trash-alt", class: "fa-thin"), article_path(article),
                  method: :delete,
                  data: { confirm: '削除してよろしいですか?' }
            - else
              = render 'favorite_button', article: article
