= link_to favorite_path(current_user.favorites.find_by(article_id: article.id)),
            id: "js-favorite-button-for-article-#{article.id}",
            method: :delete,
            remote: true do
  = icon("fas", "fa-heart", class: "fa-heart")
